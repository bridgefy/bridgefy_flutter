group 'me.bridgefy.bridgefy'
version '1.0-SNAPSHOT'

buildscript {
    ext.kotlin_version = '1.7.10'
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:7.2.0'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

rootProject.allprojects {
    repositories {
        maven {
            url "http://104.196.228.98:8081/artifactory/libs-snapshot-local"
            credentials {
                username "bridgefy-beta"
                password "g\$thaP@ad8w=w=OT78_r"
            }
            authentication {
                digest(BasicAuthentication)
            }
            allowInsecureProtocol = true
        }
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

apply plugin: 'com.android.library'
apply plugin: 'kotlin-android'

android {
    compileSdkVersion 31

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = '1.8'
    }

    sourceSets {
        main.java.srcDirs += 'src/main/kotlin'
    }

    defaultConfig {
        minSdkVersion 21
    }
}

dependencies {
    implementation (group: "me.bridgefy", name: "android-sdk", version: "0.4.1-SNAPSHOT", ext: "aar") {
        transitive = true
    }
    implementation "androidx.preference:preference-ktx:1.2.0"
    implementation "androidx.lifecycle:lifecycle-extensions:2.2.0"
}
